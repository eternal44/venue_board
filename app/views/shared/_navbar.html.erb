<header class="navbar navbar-default navbar-fixed-top" role="banner">
  <div class="container">
    <div class="navbar-header">
      <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/" class="navbar-brand">Home</a>
    </div>
    <nav class="collapse navbar-collapse" role="navigation">
      <ul class="nav navbar-nav">
        <% if employer_signed_in? %>
          <li><%= link_to "Logout", destroy_employer_session_path, method: :delete %></li>
          <li><%= link_to "Account", edit_employer_registration_path %></li>
        <% elsif worker_signed_in? %>
          <li><%= link_to "Logout", destroy_worker_session_path, method: :delete %></li>
          <li><%= link_to "Account", edit_worker_registration_path %></li>
        <% elsif admin_signed_in? %>
          <li><%= link_to "Logout", destroy_admin_session_path, method: :delete %></li>
        <% else %>
        <% end %>
      </ul>
    </nav>
  </div>
</header>

